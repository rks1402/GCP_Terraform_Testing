steps:
- name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    apt update
    apt install -y postgresql-client
    psql -h <34.82.222.77> -U $USERNAME -p $PASSWORD -d <my-database> "table.sql"