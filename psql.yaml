steps:
- name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    apt update
    apt install -y postgresql-client
    psql -U <root> -p <root> <my-database> < "./table.sql"